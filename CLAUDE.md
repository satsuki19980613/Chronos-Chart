# Chronos Chart — セッション開始時の指示

日本株の株価・テクニカル指標・需給・法定開示を1つのチャートで突き合わせるローカル GUI ツール。
土台は Autotechnical（pywebview + yfinance + SQLite + Lightweight Charts v5）。実装は複数セッションに分けて進める。

## 最初に必ず読む

1. `docs/PLAN.md` の §0（運用ルール）・§1（現在地）・§4（申し送り事項）・§5（未解決事項）
2. 着手するタスクに対応する `docs/SPEC.md` のセクション

**仕様の正は `docs/SPEC.md`。** `docs/RESEARCH.md` と `docs/DESIGN.md` はレビュー前の記述を含み、
冒頭の訂正表にあるとおり一部に事実誤認がある。食い違ったら必ず SPEC を優先する。

## 進め方

- タスクは `docs/PLAN.md` §2 から「状態が `TODO` で依存がすべて `DONE` の最小 ID」を選ぶ（`WIP` があれば先に片付ける）
- **タスクを完了したら、その実装と同じコミットで** PLAN の状態列と §1「次にやること」を更新する（セッション終了時にまとめない）
- 仕様を変えた・決めたら SPEC 本文と SPEC §12 変更履歴（何を・なぜ）、PLAN §6 決定記録を更新する
- テスト: `python -m pytest`（ネットワーク無しで完走すること。実通信テストは `CHRONOS_LIVE=1` のときだけ）
- コミットのメールアドレスはリポジトリ設定の GitHub noreply アドレスを使う（個人アドレスだと GitHub が push を拒否する）
- push・main へのマージはユーザーの指示を受けてから行う

## 不変条件（破ると規約違反か、静かにデータが壊れる）

1. **需給データ（空売り残高・貸借取引残高）を AI に送らない。レポートにもエクスポートにも載せない。**
   プロンプトは `app/ai/prompt.py` の1関数で組み立て、`service.dashboard()` の payload を流用しない
2. **第三者サイトの実データを Git にコミットしない。** フィクスチャは合成データ。実物は `tests/fixtures/real/`（Git 対象外）
3. 外部サイトへのアクセスは最小限。構造確認のための採取は1回だけ。karauri.net は 10秒間隔・直列・UA 非偽装、403/429 は即中止
4. **階段線は `LWC.LineType.WithSteps` と定数で書く**（数値の `2` は曲線）。階段にするのは空売り残高合計だけ。貸借取引残高は欠測をまたいで結ばない
5. **EDINET の突合は EDINET コードで行う**（`secCode` は提出者のコード）。`documents.json` は銘柄で絞る前に日次キャッシュへ保存する
6. 開示のラベルに「決算」を使わない（EDINET にあるのは有報・半期報で、決算発表日ではない）
7. マーカーの日付は DB に持たず、表示時に `prices.date` から決める。祝日表を作らない
8. APIキーは `keyring` に保存し、DB・ログ・レポートに出さない
9. `google-genai` の `retry_options` を設定しない。送信を試みるたびに `ai_usage.requests` を加算する。AI 分析は自動実行しない
10. 長時間処理はジョブで実行し、HTTP 待ちの間に DB の書き込みロックを持たない
11. 自動更新は起動時の1回だけ。失敗してもダイアログで起動を妨げない
12. `margin_balances`（貸借取引残高）は再取得できない。マイグレーションで DROP しない
13. UI で「信用残」と単独表記しない（「貸借取引残高（日証金）」）

詳細と理由は `docs/PLAN.md` §4 と `docs/SPEC.md`。
